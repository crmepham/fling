# Port the backend listens on (default: 8080)
BACKEND_PORT=8080

# Port the frontend (nginx) listens on (default: 80)
FRONTEND_PORT=80

# Fling login credentials (default: admin / fling)
FLING_USERNAME=admin
FLING_PASSWORD=fling
